
model User {
    id        String   @id @default(uuid())
    name      String
    email     String   @unique
    phone     String
    github    String
    linkedin  String
    quote     String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

    